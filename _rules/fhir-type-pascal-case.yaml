---
description: FHIR "types" must be PascalCase
message: '{{property}} is not PascalCase: {{error}}'
severity: warn
recommended: true
format: oas2
given: $.paths[*]~
then:
  function: pattern
  functionOptions:
    match: ^(\/)?[A-Z][a-zA-Z0-9]*(\/)?
...description: FHIR "types" must be PascalCase
message: '{{property}} is not PascalCase: {{error}}'
severity: warn
recommended: true
format: oas2
given: $.paths[*]~
then:
  function: pattern
  functionOptions:
    match: ^(\/)?[A-Z][a-zA-Z0-9]*(\/)?