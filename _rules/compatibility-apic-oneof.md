---
description: API Connect has a compatibility issue with the oneOf functionality. Avoid
  its use if reasonably possible.
given: $..
severity: warn
then:
  field: oneOf
  function: falsy
...description: API Connect has a compatibility issue with the oneOf functionality. Avoid
  its use if reasonably possible.
given: $..
severity: warn
then:
  field: oneOf
  function: falsy