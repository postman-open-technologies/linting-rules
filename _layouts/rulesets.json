---
layout: none
---
{
"rules": {
    {% assign rules = site.rules %}{% for this_rule in page.rules %}{% for rule in rules %}{% if this_rule == rule.slug %}"{{ rule.slug }}": {{ rule.content | escape | strip_html | jsonify }}{% endif %}{% endfor %}{% unless forloop.last %},{% endunless %}{% endfor %}
}}